apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: wake-cycle-agent

namespace: kagent

resources:
  - agent.yaml
  - cronjob.yaml
  - storage.yaml
  - toolserver.yaml

commonLabels:
  app: wake-cycle-agent
  project: mcp-hackathon

# Optional: Add your image override
# images:
# - name: ghcr.io/optimus-fulcria/wake-cycle-tools
#   newTag: v1.0.0
